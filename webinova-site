<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WebInova - Design Web que Inova</title>
    <style>
        :root {
            --bg-color: #000000;
            --text-color: #ffffff;
            --accent-color: #d10056; /* Ruby */
            --secondary-color: #00a86b; /* Jade */
        }
        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-color);
            overflow-x: hidden;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
        }
        header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            backdrop-filter: blur(10px);
            background: rgba(0, 0, 0, 0.5);
        }
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: var(--accent-color);
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }
        .nav-links a {
            color: var(--text-color);
            text-decoration: none;
            margin-left: 20px;
            transition: color 0.3s ease;
        }
        .nav-links a:hover {
            color: var(--accent-color);
        }
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
            text-align: center;
        }
        .hero-content {
            z-index: 2;
            opacity: 0;
            filter: blur(10px);
            transition: opacity 2s ease, filter 2s ease;
        }
        .text {
            font-size: 3rem;
            margin-bottom: 20px;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            font-weight: bold;
            white-space: nowrap;
            overflow: hidden;
            border-right: .15em solid var(--text-color);
            display: inline-block;
            animation: blink-caret 0.75s step-end infinite;
        }
        .highlight-web, .highlight-inova {
            transition: color 1s ease;
        }
        .highlight-web {
            color: var(--text-color);
        }
        .highlight-inova {
            color: var(--text-color);
        }
        @keyframes blink-caret {
            from, to { border-color: transparent }
            50% { border-color: var(--text-color); }
        }
        .blob {
            position: absolute;
            border-radius: 50%;
            filter: blur(60px);
            transition: all 2s ease;
        }
        .blob1, .loading-circle1 {
            width: 8rem;
            height: 8rem;
            background-color: var(--accent-color);
        }
        .blob2, .loading-circle2 {
            width: 5rem;
            height: 5rem;
            background-color: var(--secondary-color);
        }
        .blob1 {
            top: 40%;
            left: 5%;
            transform: translate(0, 0);
        }
        .blob2 {
            bottom: 40%;
            right: 5%;
            transform: translate(0, 0);
        }
        .cta-button {
            display: inline-block;
            background-color: var(--accent-color);
            color: var(--bg-color);
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 25px;
            font-weight: bold;
            transition: all 0.3s ease;
            margin-top: 20px;
        }
        .cta-button:hover {
            background-color: var(--secondary-color);
            transform: scale(1.05);
        }
        .services {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            margin-top: 50px;
            text-align: center;
        }
        .service-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 20px;
            margin: 20px;
            transition: transform 0.3s ease;
            flex-basis: calc(33% - 40px);
        }
        .service-card:hover {
            transform: translateY(-10px);
        }
        footer {
            text-align: center;
            padding: 50px 0;
            margin-top: 50px;
        }
        .parallax {
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
        /* Loading Animation */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: var(--bg-color);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            opacity: 1;
            transition: opacity 0.5s ease;
        }
        .loading-animation {
            position: relative;
        }
        .loading-circle {
            position: absolute;
            border-radius: 50%;
            filter: blur(20px);
            animation: dance 1.5s infinite alternate;
        }
        .loading-circle1 {
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation-delay: 0s;
        }
        .loading-circle2 {
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation-delay: 0.5s;
        }
        @keyframes dance {
            from {
                transform: scale(1) translate(-50%, -50%);
            }
            to {
                transform: scale(1.5) translate(-50%, -45%);
            }
        }
        @-webkit-keyframes dance {
            from {
                -webkit-transform: scale(1) translate(-50%, -50%);
            }
            to {
                -webkit-transform: scale(1.5) translate(-50%, -45%);
            }
        }
    </style>
</head>
<body>
    <div class="loading-screen">
        <div class="loading-animation">
            <div class="loading-circle loading-circle1"></div>
            <div class="loading-circle loading-circle2"></div>
        </div>
    </div>

    <header>
        <nav>
            <div class="logo">WebInova</div>
            <div class="nav-links">
                <a href="#home">Home</a>
                <a href="#services">Serviços</a>
                <a href="#contact">Contato</a>
            </div>
        </nav>
    </header>

    <main>
        <section id="home" class="hero parallax">
            <div class="blob blob1"></div>
            <div class="blob blob2"></div>
            <div class="hero-content">
                <div class="text" id="animated-text"></div>
                <p>Transforme sua presença online com designs únicos e impactantes</p>
                <a href="#contact" class="cta-button">Encomende seu site</a>
            </div>
        </section>

        <section id="services" class="container">
            <h2>Nossos Serviços</h2>
            <div class="services">
                <div class="service-card">
                    <h3>Web Design</h3>
                    <p>Criamos sites responsivos e modernos que capturam a essência da sua marca.</p>
                </div>
                <div class="service-card">
                    <h3>E-commerce</h3>
                    <p>Desenvolvemos lojas online intuitivas e seguras para impulsionar suas vendas.</p>
                </div>
                <div class="service-card">
                    <h3>SEO</h3>
                    <p>Otimizamos seu site para os mecanismos de busca, aumentando sua visibilidade online.</p>
                </div>
            </div>
        </section>
    </main>

    <footer id="contact" class="parallax">
        <h2>Entre em Contato</h2>
        <p>Estamos prontos para transformar sua visão em realidade digital.</p>
        <a href="mailto:contato@webinova.com" class="cta-button">Contate-nos</a>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const loadingScreen = document.querySelector('.loading-screen');
            const loadingCircle1 = document.querySelector('.loading-circle1');
            const loadingCircle2 = document.querySelector('.loading-circle2');
            const blob1 = document.querySelector('.blob1');
            const blob2 = document.querySelector('.blob2');
            const heroContent = document.querySelector('.hero-content');
            const textElement = document.getElementById('animated-text');

            const typeText = async (element, text, delay = 100) => {
                for (let i = 0; i < text.length; i++) {
                    element.innerHTML += text[i];
                    await new Promise(resolve => setTimeout(resolve, delay));
                }
            };

            const deleteText = async (element, delay = 50) => {
                while (element.innerHTML.length > 0) {
                    element.innerHTML = element.innerHTML.slice(0, -1);
                    await new Promise(resolve => setTimeout(resolve, delay));
                }
            };

            const animateText = async () => {
                const logoText = 'WebInova';
                const fullText = 'Design Web que Inova';

                await typeText(textElement, logoText, 150);
                await new Promise(resolve => setTimeout(resolve, 1000));
                await deleteText(textElement, 100);
                await new Promise(resolve => setTimeout(resolve, 500));
                textElement.innerHTML = '';
                await typeText(textElement, fullText, 100);

                // Add color transition
                await new Promise(resolve => setTimeout(resolve, 1000));
                document.querySelector('.highlight-web').style.color = 'var(--secondary-color)';
                document.querySelector('.highlight-inova').style.color = 'var(--accent-color)';
            };

            setTimeout(() => {
                loadingScreen.style.opacity = '0';
                setTimeout(() => {
                    loadingScreen.style.display = 'none';

                    // Match loading circles to final blobs
                    blob1.style.width = loadingCircle1.style.width;
                    blob1.style.height = loadingCircle1.style.height;
                    blob1.style.backgroundColor = loadingCircle1.style.backgroundColor;
                    
                    blob2.style.width = loadingCircle2.style.width;
                    blob2.style.height = loadingCircle2.style.height;
                    blob2.style.backgroundColor = loadingCircle2.style.backgroundColor;

                    // Transition blobs to final positions and sizes
                    blob1.style.width = '400px';
                    blob1.style.height = '400px';
                    blob1.style.top = '10%';
                    blob1.style.left = '5%';
                    blob1.style.transform = 'translate(0, 0)';

                    blob2.style.width = '300px';
                    blob2.style.height = '300px';
                    blob2.style.bottom = '10%';
                    blob2.style.right = '5%';
                    blob2.style.transform = 'translate(0, 0)';

                    heroContent.style.opacity = '1';
                    heroContent.style.filter = 'blur(0)';

                    // Start text animation
                    animateText();
                }, 500);
            }, 2000);
            
            window.addEventListener('scroll', function() {
                var scrolled = window.scrollY;
                var parallaxElements = document.querySelectorAll('.parallax');
                parallaxElements.forEach(function(el) {
                    var speed = 0.5;
                    el.style.backgroundPositionY = -(scrolled * speed) + 'px';
                });
            });
        });
    </script>
</body>
</html>
